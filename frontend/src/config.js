// Configuración central de la API
// Si cambias la URL del backend, solo cambia este archivo.
const API_URL = import.meta.env.VITE_API_URL || 'https://sistema-rentas-backend.onrender.com';

export default API_URL;
